"""Setup script for python-codemode."""
from setuptools import setup, find_packages

setup(
    name="python-codemode",
    version="0.1.0",
    packages=find_packages(include=["python_codemode", "python_codemode.*"]),
)
